/*
sumOfAnArray
static method which sums up all the elements (integers) in an array
maxValueInArray
static method which returns the max value in an array of integers 
isStringInArray
static method which returns true if a string exists in an array, false otherwise
Call each method 3 times from main (provide different arguments each time)
*/